import type { BundledLanguage, BundledTheme } from "shiki";

export const CODE_BLOCKS: {
  THEME: BundledTheme;
  LANGUAGE: BundledLanguage;
  COPY_BUTTON: boolean;
} = {
  THEME: "dracula",
  LANGUAGE: "tsx",
  COPY_BUTTON: true,
};
